package Ovulam;

public class CreatorsStates {
/*    GameState gameState;
    public boolean isFingt;
    private FightState state;

    public void set(FightState astate){
        if(state == astate) return;

        Events.fire(new FightStateChangeEvent(state, astate));
        state = astate;
    }


    public boolean isNormal(){
        return state == FightState.normal;
    }

    //第一区块的预备
    public boolean isInPreparation(){
        return state == FightState.prepareTime;
    }

    //第二区块的战斗
    public boolean isFighting(){
        return state == FightState.fightingBOSS;
    }

    public enum FightState{
        normal, prepareTime, fightingBOSS;
    }

    public static class FightStateChangeEvent{
        public final FightState from, to;

        public FightStateChangeEvent(FightState from, FightState to) {
            this.from = from;
            this.to = to;
        }
    }*/
}
